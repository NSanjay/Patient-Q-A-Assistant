version: '3.8'
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: patientqa
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
